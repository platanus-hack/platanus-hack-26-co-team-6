# core configuration. Copy to .env and adjust.
# Every value has a working default in code, so the service boots with no .env.

# Port Nest binds. Default: 3001 (3000 belongs to apps/frontend)
PORT=3001

# Browser origin allowed by CORS. Default: http://localhost:3000
# NUNCA '*': core manda cookie de sesión, y con credenciales el navegador
# exige un origen exacto.
CORS_ORIGIN=http://localhost:3000

# Servicio interno de IA. Si está puesta, POST /triage intenta resolver el
# dictado en ai-core ANTES de resolverlo localmente. Si ai-core no responde,
# tarda de más o devuelve basura, core sigue con su camino de siempre — la
# variable no agrega un punto de falla, agrega un primer intento.
#
# Déjala vacía o coméntala para que core resuelva todo local.
# El campo `via` de la respuesta dice cuál corrió.
AI_CORE_BASE_URL=http://localhost:8000

# Presupuesto para una llamada de inferencia a ai-core. Default: 30000.
# Pasado esto, core cae a su camino local en vez de esperar. El probe de
# /health/ai-core usa 2s fijos y no se configura.
# AI_CORE_TIMEOUT_MS=30000

# ── Sesión de operador ────────────────────────────────────────────
# core NO se sirve sin autenticar: expone dictado clínico, diagnóstico y las
# coordenadas del paciente. Si dejas estas dos en blanco, arranca igual pero
# genera credenciales aleatorias y las imprime en el log — nunca queda abierto.

# Contraseña compartida del turno. La piden /campo, /hospital y /crue.
# OPERADOR_PASSWORD=

# Clave para firmar la cookie de sesión (HMAC). Mínimo 16 caracteres.
#   openssl rand -hex 32
# Si falta, se usa una aleatoria en memoria: las sesiones mueren al reiniciar.
# SESION_SECRET=

# Ponlo en 'true' SOLO si front y core viven en dominios distintos. Cambia la
# cookie a SameSite=None y exige HTTPS. En localhost déjalo fuera.
# COOKIE_CROSS_SITE=false

# ── Rotación del secreto de firma ─────────────────────────────────
# Rotar SESION_SECRET invalida de golpe TODO lo firmado con el anterior,
# incluido el token de 24 h que `voz` lleva en su variable de entorno: serían
# minutos de webhooks de WhatsApp cayéndose. Para que rotar no tumbe nada:
#   1. el secreto viejo pasa aquí,
#   2. el nuevo va en SESION_SECRET,
#   3. cuando cierre la ventana, se borra esta línea.
# Se ACEPTA para verificar; nunca se firma con él.
# SESION_SECRET_ANTERIOR=

# Cuánto dura esa ventana, contada desde el arranque del proceso — que es el
# despliegue que hizo la rotación. Default: 24 h, la vida de un token de
# servicio: pasado eso no queda ninguno firmado con el viejo.
# PULSO_SECRETO_ANTERIOR_HORAS=24

# ── Credencial de plataforma ──────────────────────────────────────
# Abre POST /auth/servicio, que emite los tokens de servicio (hoy: `svc:voz`).
# Mínimo 16 caracteres:  openssl rand -hex 32
#
# SIN ESTA VARIABLE LA RUTA NIEGA A TODO EL MUNDO. Es la excepción a la regla
# de degradación del repo: emitir una identidad con alcance sobre datos
# clínicos no puede caer a un modo abierto, y tampoco se genera una credencial
# aleatoria como con OPERADOR_PASSWORD — sin credencial, la puerta no existe.
#
# Va en su propia cabecera (x-pulso-admin-token), no en Authorization: la
# sesión de un operador NO sirve para fabricar bots. Cuando llegue la tarea
# 1.3 esto lo reemplaza el rol admin_plataforma.
#
#   curl -X POST http://localhost:3001/auth/servicio \
#        -H "x-pulso-admin-token: $PULSO_ADMIN_TOKEN" \
#        -H 'content-type: application/json' -d '{"nombre":"voz"}'
#
# El token que devuelve va en CORE_SERVICE_TOKEN de apps/services/voz/.env
# PULSO_ADMIN_TOKEN=

# ── Estado de ruteo ───────────────────────────────────────────────
# Dónde vive el estado del ruteo (casos, handshakes, escalamientos).
# Sin nada de esto arranca en MEMORIA y avisa en el log: el estado se pierde
# al reiniciar, igual que el almacén de sesión. Es suficiente para el demo.

# Postgres para persistirlo de verdad. Si la pones, se usa.
# PULSO_ROUTING_DATABASE_URL=

# Fuerza memoria aunque haya URL arriba. Útil para ensayar sin tocar la base,
# y silencia el aviso de arranque.
# ROUTING_STORE=memory

# ── Credenciales de servidor ──────────────────────────────────────
# Estas NUNCA van en apps/frontend: se saltan RLS o cuestan dinero.

# Supabase. La service role key ignora Row Level Security.
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_ANON_KEY=

# PostgreSQL directo para `task migrate*` (DDL y carga de datos).
# Supabase > Project Settings > Database > Connection string > URI.
# Usa conexión directa o session pooler en el puerto 5432. NO uses el
# transaction pooler 6543: rompe DDL y prepared statements.
# SUPABASE_DB_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres

# Mapbox Matrix API (ETA con tráfico). Sin token, estima por distancia.
# MAPBOX_TOKEN=

# Claude para el parser clínico. Sin key, cae al extractor heurístico.
# Si usas ai-core, la key puede vivir SOLO allá (apps/backend/ai-core/.env) y
# core queda sin credencial de proveedor. Es la topología que quiere el
# design.md del scaffold. Tenerla en los dos lados también funciona: core la
# usa como respaldo si ai-core no responde.
# ANTHROPIC_API_KEY=

# La transcripcion del dictado NO vive aqui: la sirve ai-core en
# POST /v1/transcribir, y su DEEPGRAM_API_KEY va en apps/backend/ai-core/.env
# GET /capacidades reporta si esta disponible.

# ── Plazos de operación ───────────────────────────────────────────
# Segundos que una solicitud espera respuesta del hospital antes de vencer y
# pasar al siguiente candidato. Default: 45 (ver src/common/plazos.ts).
# Bájalo a 5-10 para ensayar el demo sin esperar de verdad.
# HANDSHAKE_TIMEOUT_S=45

# Telegram: canal primario del handshake. Sin token, loguea a consola.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID_DEMO=

# OBLIGATORIO si usas el webhook. El webhook es la única ruta pública que
# cambia estado clínico, así que se autentica con este secreto compartido:
# lo inventas tú, se lo pasas a Telegram en setWebhook(secret_token=...) y
# core compara el header X-Telegram-Bot-Api-Secret-Token de cada entrega.
# Sin esto el webhook rechaza todo. Ver telegram.controller.ts.
#   openssl rand -hex 32
# TELEGRAM_WEBHOOK_SECRET=

# WhatsApp Cloud API: la vitrina. Requiere ventana de 24h abierta.
# WHATSAPP_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_TO_DEMO=

# URL pública del front, para los enlaces de los botones. Default: CORS_ORIGIN
# PUBLIC_APP_URL=http://localhost:3000

# ── Canal público (servicio `voz`) ───────────────────────────────
# Sin esto, core no le avisa a nadie por WhatsApp y el bucle no se cierra:
# el hospital acepta y el paramédico no se entera. Es un modo válido — el
# demo por consola y Telegram no lo necesita.
# VOZ_BASE_URL=http://127.0.0.1:8090/interno
# VOZ_SECRETO=

# ── El vigilante del reloj ───────────────────────────────────────
# Cuánto se le espera a un hospital antes de darlo por no-respuesta y
# re-rutear al siguiente candidato. Default: 60s.
# 60 es agresivo para la vida real y correcto para el escenario: el jurado
# no aguanta más, y el punto es que el sistema no se queda quieto.
# ESPERA_HANDSHAKE_S=60
